.row
  .col
    .text-center= image_tag('http://via.placeholder.com/75x75')
    .text-center #{player.previous_rating} 
    .text-center #{player.rating_delta}
  .col
    .row #{player.force.name}
    .row #{link_to player.user.full_name, user_path(player.user)}
    .row= player.winner ? "Winner" : "Loser" 
